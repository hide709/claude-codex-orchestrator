# 実行ごとの生成物(成果物・ログ。再現可能なので追跡しない)
runs/

# 自動メモリ(重複検知用の使い捨てキャッシュ。decisions.jsonl/preferences.md は追跡する)
memory/seen.jsonl

# Claude worker 連携の queue(使い捨て。常駐セッションとのファイル受け渡し)
queue/

# Python
__pycache__/
*.pyc
*.pyo

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Claude Code のローカルセッション状態(共有しない)
.claude/
